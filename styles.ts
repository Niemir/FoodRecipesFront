import { StyleSheet } from "react-native";

const inputs = StyleSheet.create({
  primary: {
    borderWidth: 1,
    borderRadius: 30,
    borderColor: "#aaa",
    padding: 10,
    maxWidth: "60%",
  },
  rounded: {
    flex: 0,
    width: 50,
  },
});

export { inputs };
